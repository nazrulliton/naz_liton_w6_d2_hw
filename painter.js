const Painter = function (name) {
  this.name = name;
  this.stock = [];
}

Painter.prototype.addCan = function (can) {
  this.stock.push(can);
};

Painter.prototype.calcVolume = function () {
  for
};

module.exports = Painter;
